# openspec/config.yaml — llm-local project
# Auto-generated by sdd-init (2026-06-11)
schema: spec-driven

context: |
  Proyecto: llm-local
  Propósito: Correr modelos de lenguaje locales en hardware modesto
  Stack: Sin código aún — proyecto vacío
  Hardware: Intel i5-7400 (4C/4T @ 3.0GHz), 7.5GB RAM, AMD RX 580 2048SP (Polaris), HDD 1TB 7200RPM
  SO: Linux
  Arquitectura detectada: CPU-only con GPU AMD para aceleración (ROCm / Vulkan)
   Modo SDD: both (openspec + engram)
   Persistencia Engram: activa — 3 memorias guardadas (init, exploración, upgrade-analysis)

strict_tdd: false
strict_tdd_reason: "No hay test runner ni código fuente — proyecto en fase de exploración/planificación"

testing:
  runner: null
  layers:
    unit: { available: false, tool: null }
    integration: { available: false, tool: null }
    e2e: { available: false, tool: null }
  coverage: { available: false, command: null }
  quality:
    linter: { available: false, command: null }
    type_checker: { available: false, command: null }
    formatter: { available: false, command: null }

hardware:
  cpu: "Intel Core i5-7400, 4C/4T @ 3.0GHz"
  ram_total_gb: 7.5
  ram_available_gb: 3.4
  gpu: "AMD Radeon RX 580 2048SP (Polaris)"
  gpu_vram_gb: 4
  gpu_cuda: false
  gpu_rocm: true
  gpu_vulkan: true
  disk: "HDD 1TB 7200 RPM"
  os: "Linux (genérico)"
  limitations:
    - "RAM limitada (~3.4GB disponible) — modelos mayores a 3B params con cuantización corren ajustados"
    - "Sin CUDA — aceleración GPU requiere ROCm o Vulkan (llama.cpp Vulkan, Ollama con ROCm)"
    - "HDD en vez de SSD — latencia en carga de modelos"
    - "Solo 4 cores / 4 threads — inferencia en CPU será lenta para modelos >1.5B"

capabilities:
  - "GPU AMD con 4GB VRAM — cuantización 4-bit/5-bit en modelos 3B-7B viable via ROCm"
  - "llama.cpp con soporte Vulkan"
  - "Ollama con backend ROCm"
  - "Modelos 1.5B-3B funcionan bien en CPU con cuantización Q4/Q5"
  - "Fine-tuning LoRA/QLoRA de modelos <3B posible con suficiente RAM (swap necesario)"
  - "Inferencia en CPU de modelos <1B para tareas muy simples"

rules:
  proposal:
    - Incluir estimación de recursos (RAM, VRAM, CPU) para cada modelo propuesto
    - Evaluar si el modelo corre en GPU, CPU, o requiere cuantización
  specs:
    - Usar Given/When/Then para escenarios
    - Especificar modelos y herramientas concretas
  design:
    - Incluir diagramas de arquitectura de modelos
    - Documentar tradeoffs de cuantización y backend
    - Incluir consumo estimado de tokens por tarea
  tasks:
    - Agrupar por fase (exploración, instalación, benchmark, afinamiento)
    - Mantener tareas completables en una sesión
  apply:
    - Seguir patrones del stack detectado
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
    criterios: "benchmark de velocidad, precisión, y consumo de recursos"
  archive:
    - Advertir antes de mergear deltas destructivos
